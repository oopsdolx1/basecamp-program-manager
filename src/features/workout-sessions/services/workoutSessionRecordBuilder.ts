import { PRINT_FORMAT, PRINT_TEMPLATE_KEY, PRINT_TEMPLATE_VERSION } from "../../printing/constants/print.constants";
import type { CreateWorkoutSessionInput, WorkoutSessionRecord, WorkoutSource } from "../domain/workoutSession.types";
const sourceFor=(input:CreateWorkoutSessionInput):WorkoutSource=>{const source=input.source??(input.programId?{type:"program",programId:input.programId}:undefined);if(!source)throw new Error("Workout source is required.");if(source.type==="program"&&(!source.programId.trim()||(input.programId&&input.programId!==source.programId)))throw new Error("Program source identity is invalid.");if(source.type==="manual"&&input.programId)throw new Error("Manual source cannot have Program identity.");return source;};
export const buildWorkoutSessionRecord=(input:CreateWorkoutSessionInput,trainerId:string,sessionId:string):Omit<WorkoutSessionRecord,"createdAt"|"updatedAt"|"printedAt"|"lastPrintedAt">=>{const exercises=[...input.exercises].sort((a,b)=>a.order-b.order),source=sourceFor(input),programId=source.type==="program"?source.programId:undefined;return{sessionId,schemaVersion:1,memberId:input.memberId,...(programId?{programId}:{}),source,trainerId,status:"created",exerciseIds:exercises.map(x=>x.exerciseId),memberSnapshot:{name:input.memberName},programSnapshot:{title:input.programTitle},exercises,prescription:{source,...(programId?{sourceProgramId:programId}:{}),sourceProgramName:input.programTitle,exercises},print:{format:PRINT_FORMAT,templateKey:PRINT_TEMPLATE_KEY,templateVersion:PRINT_TEMPLATE_VERSION,printHistoryId:null,historyIds:[],copyCount:0}};};