import type { WorkoutSessionRecord, WorkoutSource } from "../domain/workoutSession.types";
const valid=(id:unknown):id is string=>typeof id==="string"&&id.trim().length>0&&id.trim()!=="manual";
export const normalizeWorkoutSource=(value:Pick<WorkoutSessionRecord,"programId"|"source"|"prescription">):WorkoutSource=>{const explicit=value.source??value.prescription?.source;const sessionId=value.programId,prescriptionId=value.prescription?.sourceProgramId;const legacy=sessionId??prescriptionId;if(explicit){if(explicit.type==="manual"){if(sessionId||prescriptionId)throw new Error("Manual source cannot have Program identity.");return explicit;}if(explicit.type==="program"){if(!valid(explicit.programId)||!valid(legacy)||legacy!==explicit.programId||sessionId&&prescriptionId&&sessionId!==prescriptionId)throw new Error("Program source identity is invalid.");return explicit;}throw new Error("Unknown workout source.");}if(sessionId&&prescriptionId&&sessionId!==prescriptionId)throw new Error("Legacy Program source identity is invalid.");if(valid(legacy))return{type:"program",programId:legacy};throw new Error("Workout source is missing.");};